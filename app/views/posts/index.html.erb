<h1>All Posts</h1>
<!--  we iterate over each posts in @posts
# for each post we create a link with post.title (links /posts/id to post.id)
# instance var created in a controller method are avail in assoc view
# since we create and assign @posts in PostsController#index,
# we can use it in the posts index view -->
<% @posts.each do |post| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to post.title, post %>
      </h4>
    </div>
  </div>
<% end %>
